  http://Linked-Alb16-dyG23UwMony1-2135443574.us-east-1.elb.amazonaws.com
  uri = 655790570067.dkr.ecr.us-east-1.amazonaws.com/linkedin-scraper

  To deploy                                                                                                                  
                                                                                                                             
  # 1. Install & deploy infrastructure                                                                                       
    {done} cd infra && npm install && npx cdk bootstrap && npx cdk deploy                                                             
                                                                                                                             
  # 2. Build & push Docker image (use ECR URI from cdk output)                                                               
    {done} aws ecr get-login-password | docker login --username AWS --password-stdin <ECR_URI>                                        
    {done} docker build -t linkedin-scraper ..                                                                                        
    {done} docker tag linkedin-scraper:latest <ECR_URI>:latest                                                                        
    {done} docker push <ECR_URI>:latest                                                                                               
                                                                                                                             
  # 3. Set your Claude API key                                                                                               
    {done} aws secretsmanager put-secret-value --secret-id <ClaudeApiKeyArn> --secret-string 'sk-ant-...'                             
  
  # 4. Force ECS to pull the new image                                                                                       
    aws ecs update-service --cluster <cluster> --service <service> --force-new-deployment                                      
  The ALB DNS name will be printed as a stack output after cdk deploy — that's your app URL.                                 
